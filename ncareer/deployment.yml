apiVersion: apps/v1
kind: Deployment
metadata:
  name: ncareer
  namespace: ncareer
spec:
  revisionHistoryLimit: 2
  replicas: 2
  selector:
    matchLabels:
      app: ncareer
  template:
    metadata:
      labels:
        app: ncareer
    spec:
      containers:
        - name: ncareer
          image: leosunghojung/ncareer:144b1b7
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
